# team-lead.md 与 teammate.md 差异对比

本文档详细对比了 Agent Teams 中两份工具文档的差异。

---

## 概述

| 对比维度 | team-lead.md | teammate.md |
|----------|--------------|-------------|
| **目标读者** | 团队协调员（Team Lead） | 团队成员（子代理） |
| **工具数量** | 10 个 | 6 个 |
| **文件大小** | 10,225 字节 | 8,715 字节 |
| **定位** | 完整工具集 + 管理功能 | 核心协作工具集 |

---

## 工具覆盖差异

### 工具对比表

| 工具 | team-lead.md | teammate.md | 说明 |
|------|:------------:|:-----------:|------|
| **TeamCreate** | ✓ | - | 仅协调员需要创建团队 |
| **TeamDelete** | ✓ | - | 仅协调员需要删除团队 |
| **SendMessage** | ✓ | ✓ | 双方都需要通信 |
| **Agent** | ✓ | ✓ | 协调员启动成员，成员可启动子代理 |
| **TaskCreate** | ✓ | ✓ | 协调员创建任务，成员也可创建 |
| **TaskList** | ✓ | ✓ | 双方都需要查看任务列表 |
| **TaskGet** | ✓ | ✓ | 双方都需要获取任务详情 |
| **TaskUpdate** | ✓ | ✓ | 双方都需要更新任务状态 |
| **TaskStop** | ✓ | - | 仅协调员需要停止任务 |
| **TaskOutput** | ✓ | - | 仅协调员需要获取任务输出 |

### 差异分析

**team-lead.md 独有工具（4 个）：**
1. `TeamCreate` - 创建团队
2. `TeamDelete` - 删除团队
3. `TaskStop` - 停止后台任务
4. `TaskOutput` - 获取任务输出

**teammate.md 独有工具：**
- 无（所有工具都被 team-lead.md 覆盖）

---

## 内容结构差异

### team-lead.md 独有内容

| 章节 | 说明 |
|------|------|
| 团队管理工具 | TeamCreate/TeamDelete 的完整文档 |
| 任务管理工具 | TaskStop/TaskOutput 的完整文档 |
| 团队协作流程图 | 展示协调员与成员的关系 |
| 最佳实践 | 协调员视角的任务分配、消息通信、优雅终止 |

### teammate.md 独有内容

| 章节 | 说明 |
|------|------|
| 团队成员工作流程 | 6 步操作流程（TaskList → TaskGet → TaskUpdate → 执行 → TaskUpdate → SendMessage） |
| 状态流转图 | `pending → in_progress → completed → deleted` |
| 成员最佳实践 | 任务认领、任务顺序、处理阻塞、优雅退出等 7 条实践 |

---

## 流程图对比

### team-lead.md 流程图

```
┌─────────────────────────────────────────────────────────────┐
│                    Team Lead (协调员)                        │
└─────────────────────────────────────────────────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          │                   │                   │
          ▼                   ▼                   ▼
   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
   │  成员 A     │     │  成员 B     │     │  成员 C     │
   │ (blue)      │     │ (green)     │     │  (...)      │
   └─────────────┘     └─────────────┘     └─────────────┘
          │                   │                   │
          └───────────────────┼───────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │   Task List     │
                    │  (共享任务列表)  │
                    └─────────────────┘
```

### teammate.md 流程图

```
┌─────────────────────────────────────────────────────────────┐
│                    Team Lead (协调员)                        │
│              使用 TaskCreate/TaskUpdate 分配任务              │
└─────────────────────────────────────────────────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          │                   │                   │
          ▼                   ▼                   ▼
   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
   │  成员 A     │     │  成员 B     │     │  成员 C     │
   │ (blue)      │     │ (green)     │     │  (...)      │
   └─────────────┘     └─────────────┘     └─────────────┘
          │                   │                   │
          └───────────────────┼───────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │   Task List     │
                    │  (共享任务列表)  │
                    └─────────────────┘

成员典型操作流程：
1. TaskList → 查看可用任务
2. TaskGet → 获取任务详情
3. TaskUpdate → 认领任务 (设置 owner 和 status=in_progress)
4. 执行任务...
5. TaskUpdate → 完成任务 (设置 status=completed)
6. SendMessage → 向协调员汇报
```

---

## 最佳实践对比

### team-lead.md（协调员视角）

1. **任务分配**：使用 `TaskUpdate` 的 `owner` 参数将任务分配给空闲成员
2. **消息通信**：优先使用 `SendMessage` 的 `message` 类型进行一对一通信，避免滥用 `broadcast`
3. **任务依赖**：使用 `addBlocks` 和 `addBlockedBy` 设置任务依赖关系
4. **优雅终止**：使用 `shutdown_request` 和 `shutdown_response` 优雅终止团队成员
5. **后台运行**：长时间任务使用 `run_in_background: true` 启动子代理

### teammate.md（成员视角）

1. **任务认领**：从 TaskList 找到无 owner 且无 blockedBy 的任务，使用 TaskUpdate 设置 owner 为自己
2. **任务顺序**：优先处理 ID 较小的任务，因为早期任务通常为后续任务设置上下文
3. **任务更新**：开始工作前设置 `status: in_progress`，完成后设置 `status: completed`
4. **消息通信**：使用 SendMessage 与队友协调，一对一用 `message`，紧急全员通知用 `broadcast`
5. **处理阻塞**：如果任务被 blockedBy，先处理依赖任务或通知依赖任务的负责人
6. **优雅退出**：收到 shutdown_request 时，如果完成任务则 approve，否则说明原因 reject
7. **避免重复**：使用 TaskList 检查是否有其他成员已在处理类似任务

---

## 状态流转

### teammate.md 独有内容

```
pending → in_progress → completed
                      → deleted (任务不再相关)
```

---

## 使用场景对比

| 场景 | 使用文档 | 主要工具 |
|------|----------|----------|
| 创建新团队 | team-lead.md | TeamCreate |
| 启动团队成员 | team-lead.md | Agent + team_name |
| 分配任务给成员 | team-lead.md | TaskCreate → TaskUpdate(owner) |
| 查看团队任务 | 两者 | TaskList |
| 认领任务 | teammate.md | TaskUpdate(owner=self) |
| 执行任务 | teammate.md | 根据任务类型使用不同工具 |
| 汇报任务完成 | teammate.md | TaskUpdate(status=completed) + SendMessage |
| 停止成员任务 | team-lead.md | TaskStop / shutdown_request |
| 删除团队 | team-lead.md | TeamDelete |

---

## 文档关系总结

```
┌──────────────────────────────────────────────┐
│              Agent Teams 文档体系             │
├──────────────────────────────────────────────┤
│                                              │
│   ┌─────────────────┐    ┌─────────────────┐ │
│   │  team-lead.md   │    │  teammate.md    │ │
│   │   (协调员)       │    │   (成员)         │ │
│   ├─────────────────┤    ├─────────────────┤ │
│   │ • 团队管理      │    │ • 任务认领      │ │
│   │ • 完整工具集    │    │ • 任务执行      │ │
│   │ • 10 个工具     │    │ • 6 个核心工具   │ │
│   └─────────────────┘    └─────────────────┘ │
│              ↓                    ↓           │
│              └────────┬───────────┘           │
│                       ↓                       │
│            ┌─────────────────────┐            │
│            │   团队协作完成      │            │
│            └─────────────────────┘            │
└──────────────────────────────────────────────┘
```

---

*文档生成时间：2026-03-02*
